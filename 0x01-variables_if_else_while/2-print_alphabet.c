#include <studio.h>
/**
 * main - print alphabets in lowercase
 *        lowercase alphabets
 * Return: Always (Success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);

	putchar('\n');
        return (0);
}
